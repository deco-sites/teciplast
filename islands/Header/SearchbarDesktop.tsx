import Component from "$store/components/search/SearchbarDesktop.tsx";
import type { Props } from "$store/components/search/SearchbarDesktop.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
