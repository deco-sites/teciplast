import Component from "$store/components/search/PageTitle.tsx";
import type { Props } from "$store/components/search/PageTitle.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
