import Component from "$store/components/ui/Timer.tsx";
import type { Props } from "$store/components/ui/Timer.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
