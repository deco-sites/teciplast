import Component from "$store/components/header/Navbar.tsx";
import type { Props } from "$store/components/header/Navbar.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
